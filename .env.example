# Required: bearer token the cloud assistant must send. Use a long random value.
BRIDGE_TOKEN=change-me-to-a-long-random-string

# Required only for the tunnel profile (docker compose --profile tunnel up).
NGROK_AUTHTOKEN=

# Optional: path to the config file (default: config.yaml).
CONFIG_PATH=config.yaml
