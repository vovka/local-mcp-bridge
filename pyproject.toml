[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "local-mcp-bridge"
version = "0.0.1"
requires-python = ">=3.10"
dependencies = ["mcp>=1.0", "pyyaml>=6", "uvicorn>=0.30", "starlette>=0.37"]

[tool.setuptools]
packages = ["bridge"]
